@model ComandosEnvioViewModel


<div class="container">
    <form asp-action="ComandoPorLocais">
        <h3>Execução de Comandos por Localidade</h3>
        <table class="table">

            <tr>
                <td>
                    <div><h4>Comandos</h4></div>
                    <div>
                        <select name="Comandos_from" id="multiselect" class="form-control" size="5">
                            @foreach (var item in Model.Comandos)
                            {
                                <option value="@item.ComandoId">@item.Nome</option>
                            }
                        </select>
                    </div>
                </td>
            </tr>

            <tr>
                <td>
                    <div><h4>Localidades</h4></div>
                    <div>
                        <select name="Locais_from" id="multiselect" class="form-control" size="10" multiple="multiple">
                            @foreach (var item in Model.Locais)
                            {
                                <option value="@item.LocalId">@item.Nome</option>
                            }
                        </select>
                    </div>
                </td>
            </tr>
        </table>
        <table>
            <div class="row">
                <div class="col-md-2 offset-md-10">
                    <button type="submit" class="btn btn-warning  btn-sm">Executar</button>
                    <a asp-action="Index" class="btn btn-secondary btn-sm">Voltar</a>
                </div>
            </div>
        </table>
    </form>
</div>
