@model ComandosEnvioViewModel


<div class="container">
    <form asp-action="ComandoPorLista">
        <h3>Execução de Comandos por Lista</h3>
        <table class="table">

            <tr>
                <td>
                    <div><h5>Selecione o Comando</h5></div>
                    <div>
                        <select name="Comandos_from" id="multiselect" class="form-control" size="7">
                            @foreach (var item in Model.Comandos)
                            {
                                <option value="@item.ComandoId">@item.Nome</option>
                            }
                        </select>
                    </div>
                </td>
            </tr>

            <tr>
                <td>
                    <div><h5>Selectione a Lista de Terminais</h5></div>
                    <div>
                        <select name="Lista_from" id="multiselect" class="form-control" size="7" multiple="multiple">
                            @foreach (var item in Model.ListaEnvio)
                            {
                                <option value="@item.ListaEnvioId">@item.Nome</option>
                            }
                        </select>
                    </div>
                </td>
            </tr>
        </table>
        <table>
            <div class="row">
                <div class="col-md-2 offset-md-10">
                    <button type="submit" class="btn btn-warning  btn-sm">Executar</button>
                    <a asp-action="Index" class="btn btn-secondary btn-sm">Voltar</a>
                </div>
            </div>
        </table>
    </form>
</div>
