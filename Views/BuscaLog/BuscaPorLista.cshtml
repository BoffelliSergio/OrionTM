@model ComandosEnvioViewModel


<div class="container">
    <form asp-action="BuscaPorLista">
        <h3>Busca de Logs por Lista</h3>
        <table class="table">

            <tr>
                <td>
                    <div><h4>Logs</h4></div>
                    <div>
                        <select name="Logs_from" id="multiselect" class="form-control" size="5">
                            @foreach (var item in Model.Log)
                            {
                                <option value="@item.LogId">@item.Nome</option>
                            }
                        </select>
                    </div>
                </td>
            </tr>

            <tr>
                <td>
                    <div><h4>Listas de Envio</h4></div>
                    <div>
                        <select name="Lista_from" id="multiselect" class="form-control" size="10" multiple="multiple">
                            @foreach (var item in Model.ListaEnvio)
                            {
                                <option value="@item.ListaEnvioId">@item.Nome</option>
                            }
                        </select>
                    </div>
                </td>
            </tr>
        </table>
        <table>
            <div class="row">
                <div class="col-md-2 offset-md-10">
                    <button type="submit" class="btn btn-warning  btn-sm">Executar</button>
                    <a asp-action="Index" class="btn btn-secondary btn-sm">Voltar</a>
                </div>
            </div>
        </table>
    </form>
</div>
